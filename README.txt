============LOGISTIC REGRESSION & DECISION TREE==================
*Mở folder bằng VSCode*

- Các kết quả train model đã được lưu vào các file có đuôi ".pkl"
- Chỉ cần chạy file app.py, ấn vào đường link do Gradio cung cấp tại Terminal. Mỗi lần tắt sever (Ctrl C hoặc tắt VSCode) link sẽ mất, chạy lại app.py để có link mới.